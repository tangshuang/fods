{
  "name": "fods-project",
  "private": true,
  "version": "2.1.4",
  "scripts": {
    "postversion": "node scripts/post-version.js",
    "pub": "npm publish --workspaces",
    "prepublishOnly": "node scripts/pre-publish.js && npm run pub"
  },
  "workspaces": [
    "packages/*"
  ]
}
