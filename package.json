{
  "name": "fods-project",
  "private": true,
  "version": "2.1.1",
  "scripts": {
    "version": "node scripts/post-version.js",
    "pub": "npm publish --workspaces",
    "prepublishOnly": "npm run pub"
  },
  "workspaces": [
    "packages/*"
  ]
}
